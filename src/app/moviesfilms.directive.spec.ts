import { MoviesfilmsDirective } from './moviesfilms.directive';

describe('MoviesfilmsDirective', () => {
  it('should create an instance', () => {
    const directive = new MoviesfilmsDirective();
    expect(directive).toBeTruthy();
  });
});
